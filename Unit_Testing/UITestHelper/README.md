# UITestHelper

A minimal, disposable iOS app (default Xcode template + a couple of extra
storyboards/xibs) used as a sandbox for trying out UI-parsing/accessibility
tooling — such as the scripts in the `Accessibility_Python`/
`xcode-accessibility-generator` projects — against controlled, simple
storyboards/xibs instead of a full production app.

## Author

Juan Correa
